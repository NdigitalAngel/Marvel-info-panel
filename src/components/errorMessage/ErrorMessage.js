import img from './1479.gif';

const ErrorMessage = () => {
    return (
        <img src={img} alt="error" style={{ display: 'block', margin: '0 auto', width: '75px', height: '75px'}} />
    )
}

export default ErrorMessage;